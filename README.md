# Study Assistant — Frontend

## Run locally
1. npm install
2. npm run dev
3. Open http://localhost:3000

This is the UI only. The placeholder API returns a message. Next step will be to add the `/api/ai` implementation that calls OpenAI/Hugging Face.
